{
  "name": "doklo-beta",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "verify:path-swap": "tsx scripts/verify-path-swap-hardening.mjs",
    "verify:studio-cli-bridge": "pnpm build && pnpm -C apps/cli build && tsx scripts/verify-studio-cli-bridge.ts",
    "verify:onboarding-browser": "pnpm build && pnpm -C apps/cli build && pnpm -C apps/studio build:standalone && node apps/studio/e2e/onboarding-generation.mjs",
    "clean": "pnpm -r exec rm -rf dist .tsbuildinfo",
    "release:pack": "pnpm -C apps/cli run build:release && npm pack apps/cli/release --pack-destination apps/cli/release",
    "bench:models": "pnpm -C benchmarks/model-matrix bench",
    "bench:agent": "pnpm -C benchmarks/agent-context bench",
    "test:release-fault": "node --test scripts/release/__tests__/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.0",
  "dependencies": {
    "source-map-support": "^0.5.21"
  },
  "license": "Apache-2.0"
}
