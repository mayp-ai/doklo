# Sample Projects

Small Next.js / Spring Boot projects used for end-to-end testing of `@doklo-beta/adapter-*` packages.

## Planned

- `nextjs-minimal/` — single-route Next.js App Router project
- `nextjs-multi-locale/` — Next.js with `next-intl` to test Lexicon i18n binding
- `nextjs-rbac/` — auth + RBAC roles to test `roles.json` extraction
- `spring-boot-minimal/` — single controller (Phase 2)
